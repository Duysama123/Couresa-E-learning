# Chatbot Mode Configuration
# 'edge' = Vercel Edge Function (fastest, no cold start, < 500ms)
# 'backend' = Traditional server (Render, has cold start on free tier)
VITE_CHATBOT_MODE=edge

# Backend API URL (only used when CHATBOT_MODE=backend)
VITE_API_URL=http://localhost:5002
